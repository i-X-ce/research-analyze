[project]
name = "research-analyze"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "i-X-ce", email = "iceci2804@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
research-analyze = "research_analyze:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
